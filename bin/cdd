#! /usr/bin/env sh

dir=""
filter=""

if [ "$1" != "" ]; then
dir="$1"
fi

if [ "$2" != "" ]; then
filter="$2"
fi

if [ "$filter" != "" ]; then
cd "$dir" && ls | grep -i "$filter"
elif [ "$dir" != "" ]; then
cd "$dir" && ls
elif [ "$dir" = "" ]; then
cd ~ && ls
fi
